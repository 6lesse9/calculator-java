pipeline {
    agent any

    stages {
        stage ('docker_build') {
            steps {
                dir ('dockerfiles') {
                    sh 'docker build -t calculator1.0 .'
                    sh 'docker run -t -i calculator1.0'
                }
            }
        }
    }
}
